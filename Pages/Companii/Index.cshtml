@page
@model Ungureanu_Gabriel_Sebastian_CIG7.Pages.Companii.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Companii</h1>

<p>
    <a asp-page="Create">Creeaza</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Companie[0].CompanieN)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Companie[0].Anulinfiintari)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Companie[0].Adresa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Companie) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CompanieN)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Anulinfiintari)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Adresa)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
